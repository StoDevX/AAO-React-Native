import React from 'react'
import {
  StyleSheet,
  View,
  Text,
  ScrollView,
} from 'react-native'

const styles = StyleSheet.create({

})

export default class PauseMenuView extends React.Component {
  render() {
    return (
      <ScrollView>
        <View>
          <Text>The Lion's Pause Kitchen Menu</Text>
        </View>
      </ScrollView>
    )
  }
}
