diff --git a/build/swift-ui/BottomSheet/index.d.ts b/build/swift-ui/BottomSheet/index.d.ts
index d95c384a01b74fe410f430618d4ff4097e3bd1e5..3a2e0c071dcc5f3119878db804ae121258f7032d 100644
--- a/build/swift-ui/BottomSheet/index.d.ts
+++ b/build/swift-ui/BottomSheet/index.d.ts
@@ -1,16 +1,16 @@
 import { type ReactNode } from 'react';
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface BottomSheetProps extends CommonViewModifierProps {
     /**
      * The sheet's content, mounted while presented and unmounted after dismiss. Wrap it in `Group`
      * to apply presentation modifiers.
      */
-    children: ReactNode;
+    children: SwiftUIContent;
     /**
      * A view the sheet is anchored to, for example the `Button` that opens it. Rendered in place and
      * kept mounted, so presenting the sheet doesn't shift surrounding layout. Optional.
      */
-    anchor?: ReactNode;
+    anchor?: SwiftUIContent;
     /**
      * Whether the `BottomSheet` is presented.
      */
diff --git a/build/swift-ui/DisclosureGroup/index.d.ts b/build/swift-ui/DisclosureGroup/index.d.ts
index cc4aec2e3120be34495f568c29d95e2edb0c3419..94d7637b5c1e4f5901dc13ae79c7118bbf99e8cf 100644
--- a/build/swift-ui/DisclosureGroup/index.d.ts
+++ b/build/swift-ui/DisclosureGroup/index.d.ts
@@ -1,10 +1,10 @@
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface DisclosureGroupProps extends CommonViewModifierProps {
     /**
      * Text label for the disclosure group. Use `DisclosureGroup.Label` for custom label content.
      */
     label?: string;
-    children: React.ReactNode;
+    children: SwiftUIContent;
     /**
      * Controls whether the disclosure group is expanded.
      */
@@ -17,7 +17,7 @@ export interface DisclosureGroupProps extends CommonViewModifierProps {
 declare function DisclosureGroupComponent(props: DisclosureGroupProps): import("react/jsx-runtime").JSX.Element;
 declare namespace DisclosureGroupComponent {
     var Label: ({ children }: {
-        children: React.ReactNode;
+        children: SwiftUIContent;
     }) => import("react/jsx-runtime").JSX.Element;
 }
 export { DisclosureGroupComponent as DisclosureGroup };
diff --git a/build/swift-ui/Gauge/index.d.ts b/build/swift-ui/Gauge/index.d.ts
index 5d06530c8fd7b0dc9a192e91f8a0fb9656a9f382..f6230b782f2a8f04d5af13dd4050e8b2d42bdc74 100644
--- a/build/swift-ui/Gauge/index.d.ts
+++ b/build/swift-ui/Gauge/index.d.ts
@@ -1,4 +1,4 @@
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface GaugeProps extends CommonViewModifierProps {
     /**
      * The current value of the gauge.
@@ -17,19 +17,19 @@ export interface GaugeProps extends CommonViewModifierProps {
     /**
      * A label describing the gauge's purpose.
      */
-    children?: React.ReactNode;
+    children?: SwiftUIContent;
     /**
      * A label showing the current value. Use `Text` or `Label` to display the value.
      */
-    currentValueLabel?: React.ReactNode;
+    currentValueLabel?: SwiftUIContent;
     /**
      * A label showing the minimum value. Use `Text` or `Label` to display the value.
      */
-    minimumValueLabel?: React.ReactNode;
+    minimumValueLabel?: SwiftUIContent;
     /**
      * A label showing the maximum value. Use `Text` or `Label` to display the value.
      */
-    maximumValueLabel?: React.ReactNode;
+    maximumValueLabel?: SwiftUIContent;
 }
 /**
  * Renders a SwiftUI `Gauge` component.
diff --git a/build/swift-ui/Label/index.d.ts b/build/swift-ui/Label/index.d.ts
index 790498c949693231f2e6b6389055757d7ce76254..e5b942f0b1b619c23d8eee31748dc041710703ec 100644
--- a/build/swift-ui/Label/index.d.ts
+++ b/build/swift-ui/Label/index.d.ts
@@ -1,6 +1,6 @@
 import type { ColorValue } from 'react-native';
 import { type SFSymbol } from 'sf-symbols-typescript';
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface LabelProps extends CommonViewModifierProps {
     /**
      * The title text to be displayed in the label.
@@ -14,12 +14,12 @@ export interface LabelProps extends CommonViewModifierProps {
      * Custom icon view to be displayed in the label.
      * When provided, this takes precedence over `systemImage`.
      */
-    icon?: React.ReactNode;
+    icon?: SwiftUIContent;
     /**
      * Custom title view. Accepts any React node (for example, a `VStack` with title and subtitle).
      * When provided, this takes precedence over `title`.
      */
-    children?: React.ReactNode;
+    children?: SwiftUIContent;
     /**
      * The color of the label icon.
      * @deprecated Use `foregroundStyle` modifier instead.
diff --git a/build/swift-ui/LabeledContent/index.d.ts b/build/swift-ui/LabeledContent/index.d.ts
index d2147ef0c2f8aef2a7bf3464e7c8f2ab50ac254c..58ca21c32f55e2e88be396784a249b1af7fffb0e 100644
--- a/build/swift-ui/LabeledContent/index.d.ts
+++ b/build/swift-ui/LabeledContent/index.d.ts
@@ -1,10 +1,10 @@
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface LabeledContentProps extends CommonViewModifierProps {
     /**
      * The label to be displayed in the labeled content.
      */
     label?: string | React.ReactNode;
-    children: React.ReactNode;
+    children: SwiftUIContent;
 }
 /**
  * LabeledContent component uses the native [LabeledContent](https://developer.apple.com/documentation/swiftui/labeledcontent) component.
diff --git a/build/swift-ui/Mask/index.d.ts b/build/swift-ui/Mask/index.d.ts
index 87ac8184e13ccea1b9bc8a08d522e9e3e13c0f0a..72309cf861d17ab2e0c85cab717da439cb200a65 100644
--- a/build/swift-ui/Mask/index.d.ts
+++ b/build/swift-ui/Mask/index.d.ts
@@ -1,6 +1,6 @@
-import { type Alignment, type CommonViewModifierProps } from '../types';
+import { type Alignment, type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface MaskProps extends CommonViewModifierProps {
-    children: React.ReactNode;
+    children: SwiftUIContent;
     /**
      * The alignment of the mask content relative to the base content.
      * @default 'center'
@@ -8,7 +8,7 @@ export interface MaskProps extends CommonViewModifierProps {
     alignment?: Alignment;
 }
 declare function MaskContent(props: {
-    children: React.ReactNode;
+    children: SwiftUIContent;
 }): import("react/jsx-runtime").JSX.Element;
 export declare function Mask(props: MaskProps): import("react/jsx-runtime").JSX.Element;
 export declare namespace Mask {
diff --git a/build/swift-ui/Overlay/index.d.ts b/build/swift-ui/Overlay/index.d.ts
index 41bb1de27ae3570c573c510d72d5054a2c51467d..e8732897e95d0e3115d76a06d44047bd0e9cae84 100644
--- a/build/swift-ui/Overlay/index.d.ts
+++ b/build/swift-ui/Overlay/index.d.ts
@@ -1,6 +1,6 @@
-import { type Alignment, type CommonViewModifierProps } from '../types';
+import { type Alignment, type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface OverlayProps extends CommonViewModifierProps {
-    children: React.ReactNode;
+    children: SwiftUIContent;
     /**
      * The alignment of the overlay content relative to the base content.
      * @default 'center'
@@ -8,7 +8,7 @@ export interface OverlayProps extends CommonViewModifierProps {
     alignment?: Alignment;
 }
 declare function OverlayContent(props: {
-    children: React.ReactNode;
+    children: SwiftUIContent;
 }): import("react/jsx-runtime").JSX.Element;
 export declare function Overlay(props: OverlayProps): import("react/jsx-runtime").JSX.Element;
 export declare namespace Overlay {
diff --git a/build/swift-ui/Picker/index.d.ts b/build/swift-ui/Picker/index.d.ts
index 39497984f8bac26d56e2455dded6b0d73b305288..c4277397484b9948801b0c402ddcb0d5905f6535 100644
--- a/build/swift-ui/Picker/index.d.ts
+++ b/build/swift-ui/Picker/index.d.ts
@@ -1,5 +1,5 @@
 import type { SFSymbol } from 'sf-symbols-typescript';
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 type SelectionValueType = string | number | null;
 export type PickerProps<T extends SelectionValueType = any> = {
     /**
@@ -23,7 +23,7 @@ export type PickerProps<T extends SelectionValueType = any> = {
     /**
      * The content of the picker. You can use `Text` components with `tag` modifiers to display the options.
      */
-    children?: React.ReactNode;
+    children?: SwiftUIContent;
 } & CommonViewModifierProps;
 /**
  * Displays a native picker component
diff --git a/build/swift-ui/Section/index.d.ts b/build/swift-ui/Section/index.d.ts
index 67fd8f3a752d42a630b31e4b8081d414f2e47385..a30944659ecee597b36d49966389034d3704938b 100644
--- a/build/swift-ui/Section/index.d.ts
+++ b/build/swift-ui/Section/index.d.ts
@@ -1,4 +1,4 @@
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface SectionProps extends CommonViewModifierProps {
     /**
      * The title of the section.
@@ -7,15 +7,15 @@ export interface SectionProps extends CommonViewModifierProps {
     /**
      * Sets a custom footer for the section.
      */
-    footer?: React.ReactNode;
+    footer?: SwiftUIContent;
     /**
      * Sets a custom header for the section.
      */
-    header?: React.ReactNode;
+    header?: SwiftUIContent;
     /**
      * The content of the section.
      */
-    children: React.ReactNode;
+    children: SwiftUIContent;
     /**
      * Controls whether the section is expanded or collapsed.
      * When provided, the section becomes collapsible.
diff --git a/build/swift-ui/SecureField/index.d.ts b/build/swift-ui/SecureField/index.d.ts
index c638b597d7dfefbfecb17633985f6afc61997d25..93cb50b39614eb44fefc77dad2c76839823f091a 100644
--- a/build/swift-ui/SecureField/index.d.ts
+++ b/build/swift-ui/SecureField/index.d.ts
@@ -42,7 +42,7 @@ export interface SecureFieldProps extends CommonViewModifierProps {
     /**
      * Slot children - supports `<SecureField.Placeholder>` with a `<Text>` child
      */
-    children?: React.ReactNode;
+    children?: SwiftUIContent;
 }
 export type NativeSecureFieldProps = Omit<SecureFieldProps, 'text' | 'onTextChange' | 'onFocusChange'> & ViewEvent<'onTextChange', {
     value: string;
@@ -58,7 +58,7 @@ export type NativeSecureFieldProps = Omit<SecureFieldProps, 'text' | 'onTextChan
 export declare function SecureField(props: SecureFieldProps): import("react/jsx-runtime").JSX.Element;
 export declare namespace SecureField {
     var Placeholder: ({ children }: {
-        children: React.ReactNode;
+        children: SwiftUIContent;
     }) => import("react/jsx-runtime").JSX.Element;
 }
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/build/swift-ui/Slider/index.d.ts b/build/swift-ui/Slider/index.d.ts
index 41c61a99783ce7f364af69746f9af9eb7772cce6..9e791fa2fef5ca17bf4f20c4faf6a4dcd86a397f 100644
--- a/build/swift-ui/Slider/index.d.ts
+++ b/build/swift-ui/Slider/index.d.ts
@@ -1,4 +1,4 @@
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export interface SliderProps extends CommonViewModifierProps {
     /**
      * The current value of the slider.
@@ -29,15 +29,15 @@ export interface SliderProps extends CommonViewModifierProps {
     /**
      * A label describing the slider's purpose.
      */
-    label?: React.ReactNode;
+    label?: SwiftUIContent;
     /**
      * A label displayed at the minimum value position.
      */
-    minimumValueLabel?: React.ReactNode;
+    minimumValueLabel?: SwiftUIContent;
     /**
      * A label displayed at the maximum value position.
      */
-    maximumValueLabel?: React.ReactNode;
+    maximumValueLabel?: SwiftUIContent;
     /**
      * Callback triggered on dragging along the slider.
      */
diff --git a/build/swift-ui/SwipeActions/index.d.ts b/build/swift-ui/SwipeActions/index.d.ts
index ae5887b4ebdc617f1a645afcb52167bb4a9f1810..88f0452928557cdacaa1ef35572df0ca3cc46a6e 100644
--- a/build/swift-ui/SwipeActions/index.d.ts
+++ b/build/swift-ui/SwipeActions/index.d.ts
@@ -1,10 +1,10 @@
-import { type CommonViewModifierProps } from '../types';
+import { type CommonViewModifierProps, type SwiftUIContent } from '../types';
 export type SwipeActionsEdge = 'leading' | 'trailing';
 export interface SwipeActionsProps extends CommonViewModifierProps {
     /**
      * The regular content and `SwipeActions.Actions` action groups.
      */
-    children: React.ReactNode;
+    children: SwiftUIContent;
 }
 export interface SwipeActionsGroupProps {
     /**
@@ -20,7 +20,7 @@ export interface SwipeActionsGroupProps {
     /**
      * The buttons revealed when the user swipes from this edge.
      */
-    children: React.ReactNode;
+    children: SwiftUIContent;
 }
 /**
  * The buttons revealed when the user swipes the regular content from an edge.
diff --git a/build/swift-ui/TextField/index.d.ts b/build/swift-ui/TextField/index.d.ts
index 077da2f630b5379da0e3a8dd4e08f310e2a41902..40dadfb0ee3c41ba4d3b9bcb56042dc32a76e20f 100644
--- a/build/swift-ui/TextField/index.d.ts
+++ b/build/swift-ui/TextField/index.d.ts
@@ -78,7 +78,7 @@ export interface TextFieldProps extends CommonViewModifierProps {
      * (any text-styling modifiers on that `Text` are preserved as the
      * placeholder's styling).
      */
-    children?: React.ReactNode;
+    children?: SwiftUIContent;
 }
 export type NativeTextFieldProps = Omit<TextFieldProps, 'text' | 'selection' | 'onTextChange' | 'onFocusChange' | 'onSelectionChange'> & ViewEvent<'onTextChange', {
     value: string;
@@ -98,7 +98,7 @@ export type NativeTextFieldProps = Omit<TextFieldProps, 'text' | 'selection' | '
 export declare function TextField(props: TextFieldProps): import("react/jsx-runtime").JSX.Element;
 export declare namespace TextField {
     var Placeholder: ({ children }: {
-        children: React.ReactNode;
+        children: SwiftUIContent;
     }) => import("react/jsx-runtime").JSX.Element;
 }
 export { type ObservableState };
diff --git a/build/swift-ui/types.d.ts b/build/swift-ui/types.d.ts
index 939d328a77719c4d1225c522df176235acba77d2..1774b320b681396875e1b48364ece3135c70e78d 100644
--- a/build/swift-ui/types.d.ts
+++ b/build/swift-ui/types.d.ts
@@ -1,3 +1,16 @@
+/**
+ * Content passed to a SwiftUI slot.
+ *
+ * This is `ReactNode` minus `string` and `number`. A bare string reaches the
+ * native side as a `RawText` component, which the SwiftUI host has no view
+ * class for, so the app dies on mount with "ComponentView with
+ * componentHandle (RawText) not found". Wrap text in `<Text>` instead.
+ *
+ * Props that are declared `string | ReactNode` and branch on
+ * `typeof x === 'string'` (Picker/LabeledContent/ControlGroup/Menu `label`)
+ * are safe as-is and deliberately keep their original type.
+ */
+export type SwiftUIContent = React.ReactElement | ReadonlyArray<SwiftUIContent> | boolean | null | undefined;
 /**
  * The alignment of content within a container or overlay.
  * Maps to SwiftUI's `Alignment` type.
