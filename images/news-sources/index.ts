import {Image} from 'react-native'

export {ksto} from '../streaming'

export const mess = Image.resolveAssetSource(require('./mess.png') as number)
export const oleville = Image.resolveAssetSource(
	require('./oleville.png') as number,
)
export const stolaf = Image.resolveAssetSource(
	require('./stolaf.png') as number,
)
