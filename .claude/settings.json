{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$CLAUDE_TOOL_INPUT\" in *'git commit'*) if mise run claude:check 1>&2; then echo '{\"decision\": \"approve\"}'; else echo '{\"decision\": \"block\", \"reason\": \"pre-commit hooks failed. Fix the issues before committing.\"}'; fi ;; *) echo '{\"decision\": \"approve\"}' ;; esac"
          }
        ]
      }
    ]
  }
}
