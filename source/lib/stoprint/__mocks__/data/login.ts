import {LoginResponse} from '../../types'

export const mockLogin: LoginResponse = {
	authCookie: 'olethelion:cookie',
	isMobileReleaseEnabled: true,
	realName: 'olethelion',
	rememberMeEnabled: true,
	success: true,
}
